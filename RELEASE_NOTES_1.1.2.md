# HyperChanger v1.1.2

**HyperChanger 1.1.2 正式版发布！**

这是一个面向部分小米 HyperOS 4 Beta 系统的 LSPosed 自定义模块。本版本重点完善控制中心、通知与锁屏媒体体验，新增全屏锁屏音乐显示，并继续提升 System UI、相机和输入法 Hook 在不同系统构建下的兼容性与稳定性。

**新增**

* `Feature` 新增全屏音乐锁屏模式，可沉浸式显示专辑封面与媒体信息，并支持锁屏交互、时钟避让和媒体会话切换。
* `Feature` 新增控制中心编辑按钮、顶部快捷按钮和全局电源菜单的自定义 Hook，可按设置控制显示与行为。
* `Feature` 新增悬浮通知的柔光玻璃外观处理，扩展控制中心图标与通知颜色适配。

**变更与优化**

* `UI/UX` 优化锁屏媒体卡片、专辑封面圆角、拖拽/释放动画及全屏音乐进出场过渡。
* `Optimize` 改进控制中心 Hook 的延迟发现与多次重试机制，适配插件类按需加载及不同 System UI ClassLoader 结构。
* `Optimize` 优化移动网络信号/网络类型的双排布局、系统图标恢复和字体继承，减少状态栏显示错位与变色异常问题。
* `Optimize` 完善配置迁移、异常保护和多语言资源同步，继续补齐中英文/日文内置语言包与示例语言包。

**修复与重构**

* `Fix` 修复 System UI 重建或媒体会话变化后锁屏播放器封面不更新、播放器状态残留和时钟遮挡等问题。
* `Fix` 修复控制中心按钮、通知玻璃材质及 Heads-up 视图在部分 HyperOS 构建中 Hook 不生效或重复安装的问题。
* `Fix` 修复 Leica 相机解锁相关反射入口、能力检测和非首进程初始化的兼容性边界情况。
* `Fix` 修复超级小爱输入法旧版参数读取及系统重建后的配置恢复问题（建议使用超级小爱输入法-0.2.701.3366cd76）。
* `Refactor` 重构锁屏音乐、控制中心和移动网络状态栏 Hook 的发现、安装与恢复流程，降低字段差异导致的崩溃风险。

---

## 问题反馈须知

欢迎提交 Bug 和改进建议。涉及应用崩溃、异常报错、白屏或功能失效时，请附上完整的 **Logcat 日志**、设备型号、Android/HyperOS 版本、启用的作用域和复现步骤。

> 🚨 **NO LOG, NO FIX.**
>
> 无日志、无复现步骤且只有一句描述的 Issue 将不予受理并直接关闭。

感谢每一位参与测试和反馈的用户，让 HyperChanger 变得更好！

**APK SHA-256**

`320DC0FC8E19B87FA86C71B3BC5775AF4A2D8E12B56075D7A55988C8EBA75E83`
